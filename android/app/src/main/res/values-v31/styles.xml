<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Theme applied to the Android Window while the process is starting when the OS's Dark Mode setting is off -->
    <style name="LaunchTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowSplashScreenBackground">@android:color/background_light</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/splash_icon_green</item>
        <item name="android:windowSplashScreenBrandingImage">@drawable/brand</item>
    </style>
</resources>
